{
  "name": "blog-content-service",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "source": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsc -w",
    "lint": "eslint 'src/**/*.ts*'",
    "test": "jest --passWithNoTests"
  },
  "jest": {
    "preset": "jest-presets/jest/node"
  },
  "dependencies": {
    "blog-model": "workspace:*",
    "notion-model": "workspace:*",
    "logger": "workspace:*",
    "markdown-library": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "@types/node": "^18.15.10",
    "eslint-config-custom": "workspace:*",
    "jest": "^26.6.3",
    "jest-presets": "workspace:*",
    "prettier": "^2.7.1",
    "tsconfig": "workspace:*",
    "typescript": "^4.8.3"
  }
}
