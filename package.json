{
  "private": true,
  "scripts": {
    "setup": "pnpm install",
    "build": "turbo run build --filter={apps/site-nextjs}",
    "build:all": "turbo run build",
    "clean": "turbo run clean & find . -name 'node_modules' -exec rm -fr {} \\;",
    "dev": "turbo run dev --no-cache --continue --filter={apps/site-nextjs}",
    "dev:all": "turbo run dev --no-cache --continue",
    "start": "turbo run start --filter={apps/site-nextjs}",
    "start:all": "turbo run start",
    "format": "prettier '**/*.{js,jsx,ts,tsx,md}' --write",
    "lint": "turbo run lint",
    "preview": "turbo run preview --filter={apps/storybook}",
    "test": "run-p test:*",
    "test:lint": "turbo run lint",
    "test:test": "turbo run test"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^3.3.1",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.7.1",
    "turbo": "^1.8.8"
  },
  "packageManager": "pnpm@7.15.0"
}
